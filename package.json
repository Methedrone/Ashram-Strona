{
  "name": "ashram-strona",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "playwright test",
    "generate-images": "node scripts/image-gen.mjs",
    "axe-local": "node scripts/axe-run.mjs",
    "axe-check": "node scripts/axe-check.mjs",
    "audit:quick": "npm run generate-images && node scripts/jsonld-validate.mjs && node scripts/axe-run.mjs",
    "audit:ci": "npm run generate-images && npm run build && AUDIT_PORT=4321 node scripts/jsonld-validate.mjs && AUDIT_PORT=4321 node scripts/axe-run.mjs && node scripts/axe-check.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.17.1",
    "astro-indexnow": "^2.1.0",
    "astro-lightgallery": "^2.3.0",
    "astro-seo": "^1.1.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "axe-core": "^4.11.1",
    "lighthouse": "^13.0.2",
    "playwright": "^1.58.2",
    "sharp": "^0.32.5"
  }
}
