---
import Layout from '../../layouts/Layout.astro';
import SectionHeading from '../../components/SectionHeading.astro';
import { useTranslations } from '../../i18n/utils';

const lang = 'en';
const t = useTranslations(lang);

const bankAccount = {
  name: "Haidakhandi Samaj - Polska",
  number: "90 1240 6726 1111 0000 5636 8550",
  bank: "Bank PEKAO S.A.",
  nip: "894 24 30 294",
  tel: "+48 536 555 899",
  email: "babaji.poland@gmail.com"
};
---

<Layout title={t('nav.donations')} description={t('meta.donations.description')}>
  <div class="donations-page">
    <div class="container">
      <SectionHeading title={t('nav.donations')} subtitle="Support our mission" level={1} />
      
      <div class="content-wrapper">
        <div class="intro-text">
          <p>
            The Babaji Ashram in Poland is supported exclusively by voluntary donations (Dakshina). 
            Every contribution helps us maintain the center, organize events, and spread Babaji's teachings.
          </p>
          <p>
            Your support allows us to provide a place for all truth-seekers, peace, and spiritual development.
            Thank you for every form of help.
          </p>
        </div>

        <div class="donation-methods">
          <div class="method-card bank-transfer">
            <div class="icon">
              <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="5" width="20" height="14" rx="2"></rect><line x1="2" y1="10" x2="22" y2="10"></line></svg>
            </div>
            <h2>Bank Transfer</h2>
            
            <div class="account-details">
              <div class="detail-row">
                <span class="label">Recipient:</span>
                <span class="value">{bankAccount.name}</span>
              </div>
              <div class="detail-row">
                <span class="label">Account number:</span>
                <span class="value highlight">{bankAccount.number}</span>
              </div>
              <div class="detail-row">
                <span class="label">Bank:</span>
                <span class="value">{bankAccount.bank}</span>
              </div>
              <div class="detail-row">
                <span class="label">NIP (Tax ID):</span>
                <span class="value">{bankAccount.nip}</span>
              </div>
              <div class="detail-row">
                <span class="label">Phone:</span>
                <span class="value">{bankAccount.tel}</span>
              </div>
              <div class="detail-row">
                <span class="label">Email:</span>
                <span class="value">{bankAccount.email}</span>
              </div>
              <div class="detail-row">
                <span class="label">Title:</span>
                <span class="value">Donation for statutory purposes</span>
              </div>
            </div>
          </div>

          <div class="method-card other-support">
            <div class="icon">
              <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
            </div>
            <h2>Other forms of support</h2>
            <p>
              In addition to financial support, we also value help in the form of Karma Yoga (volunteer work).
              If you have construction, gardening, artistic, or other skills you would like to share,
              please contact us.
            </p>
            <a href="/en/contact" class="btn-secondary-neutral">Contact us</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .donations-page {
    padding: 4rem 0;
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .intro-text {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 4rem;
    font-size: 1.2rem;
    line-height: 1.8;
    color: var(--color-text-light);
  }

  .donation-methods {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .method-card {
    background: var(--color-bg-warm);
    border-radius: 16px;
    padding: 2.5rem;
    box-shadow: var(--shadow-soft);
    border: 1px solid var(--color-border);
    text-align: center;
    transition: transform 0.3s ease;
  }

  .method-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 30px rgba(0,0,0,0.1);
  }

  .bank-transfer {
    border-top: 4px solid var(--color-orange);
  }

  .other-support {
    border-top: 4px solid var(--color-gold);
  }

  .icon {
    width: 64px;
    height: 64px;
    background-color: var(--color-bg-alt);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    color: var(--color-text);
  }

  .bank-transfer .icon {
    color: var(--color-orange);
    background-color: var(--color-bg-alt);
  }

  .other-support .icon {
    color: var(--color-gold);
    background-color: var(--color-bg-alt);
  }

  h3 {
    font-family: var(--font-family-serif);
    font-size: 1.5rem;
    margin-bottom: 2rem;
    color: var(--color-text);
  }

  .account-details {
    text-align: left;
    background-color: var(--color-bg-alt);
    padding: 1.5rem;
    border-radius: 8px;
  }

  .detail-row {
    display: flex;
    flex-direction: column;
    margin-bottom: 1rem;
    border-bottom: 1px dashed var(--color-border);
    padding-bottom: 0.5rem;
  }

  .detail-row:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .label {
    font-size: 0.9rem;
    color: var(--color-text-light);
    margin-bottom: 0.25rem;
  }

  .value {
    font-weight: 500;
    color: var(--color-text);
    font-family: monospace;
    font-size: 1.1rem;
  }

  .highlight {
    color: var(--color-orange);
    font-weight: bold;
    font-size: 1.2rem;
  }

  .other-support p {
    color: var(--color-text-light);
    line-height: 1.6;
    margin-bottom: 2rem;
  }
</style>
